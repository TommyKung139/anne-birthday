# 李蓓雯的小站

復古無名小站風格的生日留言站（好友 + 留言板）。純靜態單頁，資料存在 Firebase Firestore。

- `index.html`：整個網站（含 Firebase 設定）
- `firestore.rules`：Firestore 安全規則（更新後要到 Firebase 主控台 → Firestore → 規則 貼上並發布）

## 部署
Vercel 連結此 GitHub repo，Framework 選 Other，無需 build。push 到 main 就會自動重新部署。
